# Azure CICO connection (built-in profile)
azure_cico_server=your_server_name
azure_cico_database=your_database_name
azure_cico_username=your_username_here
azure_cico_password=your_password_here

# Add new environment variables for each connection profiles here
# Example:
# local_server=your_server_name
# local_database=your_database_name
# local_username=your_username
# local_password=your_password

##############################################
# 📌 Notes for Collaborators
##############################################

# This file is a reference for required environment variables.
# The actual .env file is gitignored and should NOT be committed.

# ➕ Whenever you add a new environment variable to your .env file,
#    make sure to update this .env.example file too!
#    That way, others know exactly which secrets/configs are needed.

# ✅ To use: copy this file and rename it to `.env`,
#    then fill in your own credentials.